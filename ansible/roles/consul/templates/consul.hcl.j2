datacenter = "homelab"
data_dir   = "/opt/consul"
log_level  = "INFO"
node_name  = "{{ inventory_hostname }}"

server           = true
bootstrap_expect = 1

client_addr = "0.0.0.0"
bind_addr   = "0.0.0.0"

ui_config {
  enabled = true
}