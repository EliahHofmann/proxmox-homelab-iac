---
# Feste Version statt rolling "release": ein Deploy soll nicht ungefragt
# den naechsten Major ziehen (v3 hat pgvecto.rs entfernt).
immich_version: "v3.1.0"
immich_install_path: "/opt/immich"
immich_upload_dir: "/opt/immich/data"
immich_db_password: "{{ lookup('env', 'IMMICH_DB_PASSWORD') }}"
immich_port: 2283
metrics_port: 8081